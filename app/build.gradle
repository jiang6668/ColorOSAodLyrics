plugins {
    id "com.android.application"
}

android {
    namespace "com.codex.colorosaodlyrics"
    compileSdk 34

    defaultConfig {
        applicationId "com.codex.colorosaodlyrics"
        minSdk 28
        targetSdk 34
        versionCode 1
        versionName "0.1.0"
    }
}

dependencies {
    compileOnly files("libs/xposed-api-stubs.jar")
}
